<section class="about" id="sobre-mi">
  <div class="about__container">
    <div class="about__media">
      <img
        class="about__img"
        src="images/perfil.webp"
        alt="Foto de Elias"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div class="about__content">
      <h2>Sobre mí</h2>
      <p class="about__lead">
        Me apasiona aprender nuevos lenguajes y tecnologías, disfruto
        enfrentarme a desafíos complejos y encontrar soluciones que no solo
        funcionen hoy, sino que sean eficientes, escalables y sostenibles en el
        tiempo.
      </p>
      <p>
        Siempre estoy buscando superar algún reto personal, aplicar nuevas
        tecnologías y desarrollarme como persona.
      </p>

      <ul class="about__highlights" aria-label="Destacados">
        <li>+6 años construyendo sitios webs accesibles y dinámicos</li>
        <li>Diseño responsivo y design systems</li>
        <li>SEO técnico y métricas Core Web Vitals</li>
        <li>3 años en soluciones de frame banking (PTS, MAMBU)</li>
      </ul>

      <!-- TODO: Actualizar CV y dejarlo acá XDXD no leas esto -->
      <!-- <div class="about__actions">
        <a class="btn" href="/cv.pdf">Descargar CV</a>
      </div> -->
    </div>
  </div>
</section>

<style>
  #sobre-mi {
    background: var(--bg);
    box-shadow: 20px 0 40px var(--bg);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .about__container {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    gap: clamp(1rem, 2.5vw, 2rem);
    align-items: center;
  }

  .about__media {
    position: relative;
    isolation: isolate;
  }
  .about__img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border-radius: 1.25rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
    border: 1px solid var(--ring);
  }
  .about__img:hover {
    animation-name: shake;
    animation-duration: 0.2s;
    animation-timing-function: ease-in-out;
    animation-delay: 0s;
    animation-iteration-count: 2;
  }

  .about__media::after {
    content: "";
    position: absolute;
    inset: -6px;
    border-radius: 1.4rem;
    border: 2px dashed color-mix(in oklab, var(--accent), transparent 70%);
    pointer-events: none;
  }

  .about__lead {
    font-size: clamp(1rem, 0.9rem + 0.5vw, 1.125rem);
    color: var(--muted);
    margin: 0 0 0.75rem;
  }
  .about__content p {
    margin: 0 0 0.75rem;
  }

  .about__highlights {
    display: grid;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    margin: 1rem 0;
    background: var(--card);
    border: 1px solid var(--ring);
    border-radius: 1rem;
    list-style: none;
  }
  .about__highlights li {
    position: relative;
    padding-left: 1.25rem;
  }
  .about__highlights li::before {
    content: "•";
    position: absolute;
    left: 0;
    top: 0;
    line-height: 1;
    transform: translateY(2px);
    color: var(--accent);
    font-weight: 700;
  }

  .about__actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-top: 0.5rem;
  }
  .btn {
    display: inline-block;
    padding: 0.7rem 1rem;
    border-radius: 0.8rem;
    text-decoration: none;
    font-weight: 600;
    border: 1px solid var(--accent);
    background: var(--accent);
    color: white;
    transition:
      transform 0.08s ease,
      box-shadow 0.2s ease,
      background 0.2s ease;
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.15);
  }
  .btn:active {
    transform: translateY(1px);
  }
  .btn--ghost {
    background: transparent;
    color: var(--accent);
    box-shadow: none;
  }

  @media screen and (min-width: 600px) {
    #sobre-mi {
    }

    .about__media {
      max-width: 320px;
    }
  }

  /* Dos columnas a partir de 700px */
  @media (min-width: 992px) {
    .about__container {
      grid-template-columns: minmax(260px, 320px) 1fr;
    }
  }

  /* Reduce motion si el usuario lo prefiere */
  @media (prefers-reduced-motion: reduce) {
    .btn {
      transition: none;
    }
  }
</style>
